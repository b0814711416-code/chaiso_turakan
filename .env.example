# คัดลอกค่า connection string จาก Neon (neon.tech) มาวางตรงนี้
# ในเครื่องตัวเองให้สร้างไฟล์ชื่อ .env.local แล้วใส่บรรทัดนี้
DATABASE_URL="postgresql://user:password@host.neon.tech/dbname?sslmode=require"
